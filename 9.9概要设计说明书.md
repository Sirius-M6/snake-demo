# 9.9 概要设计说明书

| 项 | 内容 |
| --- | --- |
| 版本 | v0.1(草案) |
| 日期 | 2026-09-09 |
| 作者 | 郑依依 |
| 状态 | **设计草案:接口化代码尚未落地 src,待晨会与 B/C 模块确认后实施** |
| 关联文档 | PRD.md(v1.6)、README.md(分工与模块约定)、9.8需求文档.md(v1.6) |

---

## 1. 目的与范围

本文档承接需求阶段成果,完成**概要设计**:

1. 收编包结构与模块依赖(以 9.9 实际代码为准,修订 README 规划期描述);
2. 给出核心实体**接口化拆分**的完整决策(拆/不拆逐条论证)与接口签名草案;
3. 记录**时序图/数据流图规划**(本版缓做,联调后补齐);
4. 逐条对照任务完成标准,供评审与后续晨会核对。

**范围约束**:本文档为设计草案;涉及 `model` 共享层的结构性改动(接口化)按 README 协作约定,须经当日 PM 晨会确认后另起实施;本版**不修改 src 任何文件**。

---

## 2. AI 使用与核对说明

> 任务要求:AI 生成类图与接口签名草稿,人工核对后定稿。本节为必查记录。

### 2.1 使用范围(什么由 AI 生成)

| 环节 | AI 承担 | 人工承担 |
| --- | --- | --- |
| 包结构现状盘点 | 通读全部源码,生成以代码为准的包结构表 | 逐包抽查,确认与 README 差异点 |
| 接口化候选与理由 | 按"收益准则"生成逐类决策草案(§5.1) | 逐条核对收益是否成立,否决不拆类目 |
| Snake 接口族签名 | 生成只读/可写拆分草案(§5.2) | 核对方法集是否覆盖 C 模块写操作与 view 读操作 |
| 类图/包结构图 | PlantUML 草稿 + 渲染 | 对照代码核对关系(继承/实现/依赖) |

### 2.2 人工核对记录(逐条,核对人:郑依依,日期:2026-09-09)

| # | AI 草案要点 | 核对方式 | 核对结论 |
| --- | --- | --- | --- |
| C1 | 接口化准则:多实现/测试替身或读写分离有防呆价值才拆 | 对照既有范例(ReadOnlyGameState/Store) | ✅ 通过:准则与代码注释既定风格一致 |
| C2 | Snake 拆为只读接口 + 可变实现 | 代码走查发现 `ReadOnlyGameState.snake()` 注释"只读"但返回可变类 | ✅ 通过:确认存在真实防呆漏洞,拆分必要 |
| C3 | 其余实体(Bean/Point/枚举/GameOptions 等)不拆 | 逐类检查是否有多实现需求、是否纯值/DTO | ✅ 通过:不拆论证成立(§5.1) |
| C4 | 实现类命名候选 `DequeSnake`(备选 `SnakeImpl`) | 对照 util 包先例(接口与实现同包平级命名) | ⏳ 待晨会:命名偏好与 B/C 协商 |
| C5 | 只读接口方法集(head/length/contains/body) | 核对 view 渲染与存档所需读操作覆盖 | ✅ 通过(存档读 body,渲染读 body/head) |
| C6 | 可写方法集(step/shrink/copy)归属实现类 | 核对 C 模块 GameController 注释契约 | ✅ 通过 |
| C7 | 图 1/图 2 关系线(继承/实现/依赖) | 逐一对照源码文件 | ✅ 通过 |
| C8 | 包结构修订点(controller vs service、Cell 未采用) | 对照 README 规划与真实包 | ✅ 通过:文档以代码为准(§3.1) |

> ⏳ = 需晨会/后续确认;✅ = 已核对通过。AI 生成的抽象已全部纳入上表,无未核对条目。

---

## 3. 总体架构设计

### 3.1 包结构(以代码为准)

依赖总方向:**`view` → `controller`/`model`/`config`;`controller` → `model`/`config`/`util`;`model` 为纯数据,不依赖任何包;`util` 为无界面纯工具**。

> **图注**:本版包结构图渲染异常(画面出现乱码与二维码状区域),已移除图片文件;包职责与依赖关系以上表与 §3.2 约定为准,正式图随 §8 R5 联调后补齐。

| 包 | 职责 | 主要维护模块 | 与 README 规划差异(9.9 核对) |
| --- | --- | --- | --- |
| `org.example` | 程序入口 MainApp(场景装载) | 全体(PM) | 无 |
| `config` | 数值/难度/速度/地图集/界面尺寸参数,集中管理 | B、C(A:MapCatalog、UiConfig) | 无 |
| `model` | 纯数据模型与状态聚合,**零 JavaFX import**(§7 核查) | 三模块共享 | README 规划的 `Cell` 未采用,以 `Point` 承担坐标 |
| `util` | 通用工具与持久化抽象 | B | 无 |
| `controller` | 规则编排:主循环/豆/输入/存档/事件接口 | C(B:BeanController、SaveController) | README 规划名为 `service`;实际为 `controller`,**本文档以代码为准收编**,不再更名(重命名代价大、收益低,待晨会背书) |
| `view` | 全部 JavaFX 界面、渲染与特效 | A | 无 |

### 3.2 架构约定(既有代码注释中已固化的规则,设计文档收编)

1. **只读视图防呆**:view 只经 `ReadOnlyGameState` 拿状态,类型层面拿不到写方法;
2. **事件解耦**:controller 触发 `GameEvents`,view 实现并订阅,禁止 view 猜测状态(diff 反模式);
3. **存储抽象**:`Store` 接口 + `LocalStore`(生产)/`InMemoryStore`(测试),规则装配在 SaveController;
4. **常量集中**:数值与参数不进代码逻辑,统一收 config;
5. **接口化收益准则**(本版新增约定):仅有"多实现/测试替身"或"读写分离防呆"价值时拆接口,纯值类型与 DTO 不拆(防过度设计)。

### 3.3 核心类图(接口化设计态)

> **图注**:本版核心类图渲染异常(画面出现乱码与二维码状区域),已移除图片文件;类间关系(继承/实现/依赖)已完整落在 §4.1 决策表与 §5 签名草案中,正式图随 §8 R5 联调后补齐。

设计要点:**虚线关系(DequeSnake、Snake 接口、GameState↔Snake 写引用)为本版草案新增**,实线为既有代码关系。

---

## 4. 核心实体接口化拆分决策(最小必要方案)

### 4.1 决策总表

| 实体 | 处置 | AI 草案理由 | 人工核对结论 |
| --- | --- | --- | --- |
| `Snake` | **拆**(只读接口 + 实现) | 见 §4.2 漏洞;测试替身需要 | ✅ 拆 |
| `GameState`/`ReadOnlyGameState` | **维持**接口+实现 | 已落地 | ✅ 维持;仅改 `snake()` 返回类型 |
| `Board` | **不拆** | 无状态几何计算器,穿墙规则唯一,无多实现需求 | ✅ 不拆(仅测试替身收益,价值低) |
| `GameMap` | **不拆(本版)** | 地图集扩充(FP-15)属 A 的 P2 演进,届时若出现多来源(字符画/程序生成)再拆 | ✅ 本版不拆,记入 §8 演进观察项 |
| `HighScoreTable` | **不拆** | 纯数据载体(DTO)+ 2 方法,拆=过度设计 | ✅ 不拆 |
| `SaveData` | **不拆** | 纯 DTO,编解码在 util.TextCodec | ✅ 不拆 |
| `GameOptions` | **不拆** | 跨页选择保持的取值对象(bean 形态) | ✅ 不拆 |
| `Bean` | **不拆** | 不可变场上豆实例(类别/位置/出生时刻),值语义 | ✅ 不拆 |
| `Point` | **不拆** | 不可变坐标值类型 | ✅ 不拆 |
| 枚举群(BeanType/Direction/Difficulty/GamePhase/GameOverReason) | **不拆** | 常量枚举,天然值类型 | ✅ 不拆 |
| `GameController` 等 controller | **不拆(本版)** | 规则编排单一实现;FP-17 竞技场若引入多控制器再议 | ✅ 本版不拆,记入 §8 |

### 4.2 必拆项:Snake 族(漏洞说明)

现代码:

```java
public interface ReadOnlyGameState {
    /** 蛇只读句柄 */
    Snake snake();   // ← 注释声称只读,返回类型却是可变类 Snake
}
public class Snake {
    public void step(Point newHead, boolean grow) { }   // view 可拿到并调用
}
```

`view` 一旦经 `state().snake()` 拿到对象,即可调用 `step/shrink` 等写操作——**只读防线在类型层面失效**。拆分后:view 拿到的类型只有读方法,写操作由 controller(经 GameState 的可写引用)调用。

### 4.3 落地方案(待晨会确认后实施,本版不改 src)

| 步骤 | 内容 | 影响面 |
| --- | --- | --- |
| 1 | 原 `Snake` 类改名为实现类 `DequeSnake`(或 `SnakeImpl`,命名晨会定),新增只读 `Snake` 接口(读方法) | `model` 内 + 潜在引用方 |
| 2 | `GameState` 持有写类型引用(controller 途经);`ReadOnlyGameState.snake()` 返回类型改为 `Snake` 只读接口 | view 调用方无感(只读用法不变) |
| 3 | `mvn compile` 全量验证 + grep 核查 `snake().step` 类越权调用 | 编译期保障 |
| 4 | B/C 规则实现(吃豆/移动)基于新签名开发 | 后续开发 |

---

## 5. Snake 接口签名草案(附录 A 全文,本版不落 src)

```java
package model;

/** 蛇只读视图(供 view 渲染/存档/碰撞查询;写方法不在本接口) */
public interface Snake {

    /** 头坐标(移动基准) */
    Point head();

    /** 当前节数 */
    int length();

    /** 蛇身是否占据该格(撞自身判定) */
    boolean contains(Point p);

    /** 身体坐标只读快照(渲染/存档);返回不可变副本 */
    java.util.List<Point> body();
}
```

```java
package model;

/** 蛇链实现:内部 Deque<Point>(头在前);写操作仅供 controller 经 GameState 调用 */
public class DequeSnake implements Snake {

    /** 头前进;grow=false 去尾(=普通走格),grow=true 保留尾(=长一节) */
    public void step(Point newHead, boolean grow) { /* 空实现骨架,规则由 C 模块实现 */ }

    /** 从尾消除 n 节(金豆效果;下限 1 节由调用方保证) */
    public void shrink(int n) { /* 空实现骨架 */ }

    /** 深拷贝(存档快照) */
    public DequeSnake copy() { return null; }   // 规则实现时填充
}
```

命名备选:实现类 `DequeSnake`(自描述内部结构)或 `SnakeImpl`(通用惯例),**晨会定稿**。

---

## 6. 核心交互与时序/数据流图(本版缓做)

### 6.1 缓做原因与完成条件

交互时序依赖 B/C 规则逻辑联调后核验(设计期消息可画,但"对应一次真实交互且无断链"需以运行期行为复核)。**完成条件:B 吃豆结算 + C 主循环首个联调版本合入后**,按 §6.2 清单补图并回填本文档。通关(FP-16,AC 待答复)、竞技场(FP-17)不列入本版。

### 6.2 已规划时序图清单(契约出处已核,消息可逐条对应方法签名)

| # | 时序图 | 主消息链(对应签名) | 契约出处 |
| --- | --- | --- | --- |
| T1 | 一帧主循环 | view 帧回调 → `GameController.tick(realNowMs)` → `stepOnce()`(私有)→ 蛇移动 `DequeSnake.step` → 渲染 `state()` 只读句柄 | GameController 注释契约 |
| T2 | 吃豆结算链 | 命中豆 → `BeanController` 效果结算 → `GameState.addScore(delta)` → `GameEvents.onBeanEaten(type,pos)` → view 飘分特效 → 补刷 `GameEvents.onBeanRefilled(bean)` | GameEvents/GameState 注释 |
| T3 | 暂停/恢复 | `GameController.pause()/resume()` → phase 迁移 → `GameEvents.onPhaseChanged(phase)` → view 暂停层显隐 | GameController 注释契约 |

### 6.3 已规划数据流图清单

| # | 数据流图 | 流经对象 |
| --- | --- | --- |
| D1 | 存档数据流(写档/读档) | GameState(SaveData 快照)⇄ `TextCodec` ⇄ `SaveController` ⇄ `Store.saveText/loadText`(Local/InMemory 分支) |
| D2 | 豆子刷新数据流 | BeanController 计时 → 超时移除/补刷 → `WeightedPicker` 比例抽取 → `Board.freeCells` 定位(避开蛇/障碍) |

---

## 7. 完成标准对照核查表

| 完成标准 | 本版状态 | 核查记录 |
| --- | --- | --- |
| Model 层零 JavaFX import | ✅ 已满足 | grep 核查:`model` 包仅 import java.*(§2.2 C7 已核);保持并写入后续开发规范 |
| 核心实体面向接口 | 🟡 草案完成,未落地 | Snake 族必拆(§4.2);其余论证不拆;待晨会后实施 |
| 时序图能对应一次真实交互且无断链 | 🟡 规划完成,本版缓做 | §6.2 契约出处已核;联调后补图验证 |
| 无职责过多的类与循环依赖 | ✅ 本版核查通过 | 全部类 ≤10 方法;依赖单向(view→controller→model;config 只被引用),见 §3.1 表 |
| AI 生成的抽象已逐条核对 | ✅ 通过 | §2.2 表 C1~C8,逐条留痕,2 条待晨会(命名/实施) |

---

## 8. 风险与待确认事项

| # | 事项 | 类型 | 责任人/时机 |
| --- | --- | --- | --- |
| R1 | 接口化(§4.3)是否实施、实现类命名(DequeSnake/SnakeImpl) | 晨会决策 | PM + B/C,9.10 晨会 |
| R2 | `controller` 包名 vs README 规划 `service` 的口径收编 | 晨会背书 | PM |
| R3 | GameMap 接口化:FP-15 地图集扩充时是否引入多来源 | 演进观察 | A 模块,FP-15 排期时 |
| R4 | GameController 接口化:FP-17 竞技场是否多控制器 | 演进观察 | PM,FP-17 拆解时 |
| R5 | T1~T3/D1~D2 图回填 | 联调后 | 轮值开发 |
| R6 | ReadOnlyGameState.snake() 漏洞修复需 B/C 开发基于新签名 | 联调约束 | C 模块 |

---

## 附录 A 说明

接口签名全文见 §5(仅 Snake 族;其余实体不拆,签名沿用现有代码,不再重复)。落地方案(§4.3)经晨会确认后,将以独立提交实施并回填本文档状态列。
