package config;

/**
 * 窗口与场景尺寸(A 模块维护)
 * 字体/配色等视觉值一律走 CSS,不在此重复(依据界面设计)
 */
public final class UiConfig {

    /** 主舞台宽度 */
    public static final double WINDOW_W = 0;

    /** 主舞台高度 */
    public static final double WINDOW_H = 0;

    /** 主舞台最小宽度 */
    public static final double MIN_W = 0;

    /** 主舞台最小高度 */
    public static final double MIN_H = 0;

    /** 私有构造:常量类不可实例化 */
    private UiConfig() {
    }
}
