# 项目架构说明书(JavaFX 贪吃蛇)

> **版本**:v1.4(草稿,待评审)
> **日期**:2026-09-11
> **维护**:专职 PM(代号 F,不承担包开发);包名/类名变更须晨会确认后同步本文档与 README
> **上游依据**:《PRD总版.md》v1.7(豆子生成与刷新规则 BR-25/BR-28/BR-29 为客户 9.10 修订版;6.1.2 功能清单 FP-01~FP-17)与《9.8需求文档.md》v1.6(BR-01~BR-78、AC-01~AC-29、QA 表;豆子规则尚未回写该文档,以 PRD v1.7 为准)
> **配套文档**:README.md(项目结构摘要、分工与协作)、PRD总版.md / 9.8需求文档.md(规则与验收)

## 1. 文档说明与修订记录

本文档描述 JavaFX 贪吃蛇的**目标包结构与类设计**:每个包装什么类、每个类有哪些方法与作用、包之间如何协作。规划内容在代码落地时可微调(注释标明"随开发落地"),但**包名、类名、职责边界、依赖方向**按本文档执行。

| 版本 | 日期 | 修订说明 | 状态 |
| ---- | ---- | ---- | ---- |
| v1.4 | 2026-09-11 | 豆子生成与刷新规则同步《PRD总版.md》v1.7(客户 9.10 补充):小豆子改为独立生成(开局 1 颗、被吃立补、恒 1 颗、不限时,不参与概率抽取);非小豆子总数上限 3 颗含小豆子(非小豆最多 2 颗)、开局按概率补满 3 颗、被吃/超时消失后 3 秒补刷 1 颗;生成概率改为 金:大=1:1、毒:大毒=1:3、正:负=3:1(QA-19 已答复,"2:1 占位"废止);大/金互斥取消(QA-20,BeanType.mutualGroup 移除);BeanController 删除 backlog 积压与保底小豆逻辑,新增 spawnSmall;§4.2/§5.6/§5.7/§5.13/§5.15/§7.3/§9.2/§10/§12/§13 同步更新 | 待评审 |
| v1.3 | 2026-09-09 | 分工机制切换:废除原"每日轮值 3 开发 + 2 测试(6 日循环)",改为**按包固定负责人制**:`config`/`model`/`util`/`controller`/`view` 由代号 A~E 各负责一包至交付 + 专职 PM(代号 F)协调;§11 重写为代号分工表,原 A/B/C 域表降级为"包内功能域参考"(域字母不再指人员,全文保留以理解类归属);README「分工与协作」与 9.9 开发日报同步 | 待评审 |
| v1.2 | 2026-09-09 | 接口化修订:model 拆只读接口 `ReadOnlyGameState`(view 类型层面只读);util 拆存储接口 `Store`(LocalStore 文件实现 + InMemoryStore 测试实现);同步 §2.4/§3.1/§5/§6/§7/§11 相关描述与归属 | 待评审 |
| v1.1 | 2026-09-09 | 格式重排:每包章首补"包结构树",类与方法改行式清单(README 102-112 风格,行尾 `#` 注释);原有表格信息按类拆分保留,无删减 | 待评审 |
| v1.0 | 2026-09-09 | 首版:按 MVC 对齐包命名(service → controller);给出 config/model/util/controller/view 五包类与方法清单、数据流时序与分工归属 | 待评审 |

## 2. 总体架构

### 2.1 技术基线

- Java 17、JavaFX 17.0.2、Maven(javafx-maven-plugin,主类 `org.example.MainApp`);
- 纯代码构建界面(不引入 FXML 场景文件),样式与主题走 CSS 与资源文件;
- 除 JavaFX 运行时外零第三方依赖;JUnit 5 仅用于测试(controller/model/util 全部不 import javafx,可脱离 UI 单测)。

### 2.2 分层与 MVC 角色对照

```
┌─────────────────────────────────────────────────────────────┐
│ view/(A)        页面类 *View、渲染、特效、CSS 主题、字体资源     │  ← 只读状态 + 事件转发
├─────────────────────────────────────────────────────────────┤
│ controller/(B、C)  规则引擎与决策 —— 即 MVC 的 Controller 主体  │  ← 输入判定/状态推进/事件发出
├─────────────────────────────────────────────────────────────┤
│ model/(共享)      纯数据与状态,不含决策逻辑,不 import javafx    │  ← MVC 的 Model
├─────────────────────────────────────────────────────────────┤
│ util/            无状态工具:时钟、加权随机、本地文件读写         │
├─────────────────────────────────────────────────────────────┤
│ config/          全部数值/地图数据集中配置,不硬编码            │
└─────────────────────────────────────────────────────────────┘
org.example/  MainApp:程序入口、全局键位采集与装配根
```

| 标准 MVC 角色 | 本项目落点 | 说明 |
| ---- | ---- | ---- |
| Model | `model` 包 | GameState/Snake/Bean…纯数据,不含"何时做什么"的决策 |
| View | `view` 包(渲染部分)+ `resources/` | 只读状态、只发事件,规则零行 |
| Controller | `controller` 包(GameController/InputController/BeanController/SaveController) | 输入判定、状态迁移、规则执行主体 |
| 事件采集 | `org.example.MainApp` + view 页面类 | JavaFX 的 KeyEvent 只能在 UI 层收到;采集后转成纯数据再交 controller,采集层本身不写规则 |

### 2.3 包总览

| 包 | 职责 | 主要维护模块 | 主要类 |
| ---- | ---- | ---- | ---- |
| `org.example` | 程序入口、依赖装配、全局键位采集 | 全体(PM 协调) | `MainApp` |
| `config` | 全部数值与地图数据集中配置(不硬编码) | A/B/C(按域) | `BoardConfig`、`BeanConfig`、`SpeedConfig`、`MapCatalog`、`SaveConfig`、`UiConfig` |
| `model` | 纯数据模型与游戏状态 | 三模块共享(PM 协调起草) | `Point`、`Direction`、`Snake`、`Board`、`GameMap`、`BeanType`、`Bean`、`Difficulty`、`GamePhase`、`GameOverReason`、`GameState`(implements `ReadOnlyGameState`)、`GameOptions`、`SaveData`、`HighScoreTable` |
| `util` | 无状态工具与基础设施 | B/C | `GameClock`、`WeightedPicker`、`Store`(接口)/`LocalStore`/`InMemoryStore`、`TextCodec` |
| `controller` | 规则引擎与决策(= MVC Controller),**不依赖 view** | B、C | `GameController`、`InputController`、`BeanController`、`SaveController`、`GameEvents`(接口) |
| `view` | 全部 JavaFX 界面、渲染、特效与资源 | A | `PageRouter`、`MainMenuView`、`GameSetupView`、`RecordsView`、`GameView`、`Palette`、`widgets/*`、`fx/*` |

> **分工注(9.9 起)**:项目为**按包固定负责人制**:`config`/`model`/`util`/`controller`/`view` 由代号 A~E 各负责一个包至交付,`org.example` 装配与整体协调由 F(专职 PM,不承担包开发)负责。本表及全文的 A/B/C 均指**功能域**(UI 美术 / 基础静态规则 / 动态规则),仅供理解类归属,**不指具体人员**;人员与职责见 §11 与 README「分工与协作机制」。

### 2.4 依赖规则与命名规范

- 依赖方向(单向):`view → controller → model/config/util`;`controller → model/config/util`;`util/config` 不依赖任何业务包;**任何层不得反向依赖 view**;
- `controller` 包 = MVC 的 Controller,类名统一 **`*Controller`** 后缀(核心主循环类命名 `GameController`,不叫 Engine);
- `view` 包 = 界面,页面类统一 **`*View`** 后缀(**不出现任何 `*Controller` 类名**,避免与 MVC Controller 双义);
- model 内 `GameState` 通过**只读接口 `ReadOnlyGameState`** 对 view 暴露(类型层面禁止调用变更方法),变更方法仅 `controller` 调用,由编译器强制;
- controller 的持久化依赖**存储接口 `Store`** 而非具体文件实现:生产注入 `LocalStore`,测试注入 `InMemoryStore`(见 §6.5);
- 新增包或重命名:由专职 PM 在晨会确认后实施,并同步本文档、README 与当日开发日报。

## 3. org.example(程序入口)

```text
org.example/                      # 程序入口与装配根(全体,PM 协调)
└── MainApp.java                  # JavaFX Application 启动类
```

### 3.1 MainApp(extends Application)

```text
MainApp.java                      # 程序入口;只做装配与键位采集转发,不含规则
├── main(String[])                # JavaFX 启动入口
├── start(Stage)                  # 装配根:SystemClock + 各 config + 四 Controller(持久化注入 Store 的 LocalStore 实现;InMemoryStore 仅测试用,不进装配)→ PageRouter 显示主界面;全局键位采集并转发
└── stop()                        # 应用退出钩子(如需清理)
```

| 方法 | 作用 |
| ---- | ---- |
| `main(String[])` | JavaFX 启动入口 |
| `start(Stage)` | 依赖装配与场景装载(方向键/空格/R/Esc 全局键位采集→纯数据转 controller);持久化注入 `Store` 的 LocalStore 实现(`InMemoryStore` 仅测试用);组装 `PageRouter` 并显示主界面 |
| `stop()` | 应用退出钩子(如需清理) |

## 4. config(参数集中配置,不硬编码)

```text
config/                           # 数值与地图数据集中配置,不硬编码
├── BoardConfig.java              # 棋盘几何:20×20、25 px、开局蛇位
├── BeanConfig.java               # 豆子:分值/蛇身效果/时限/生成概率(核心,BR-20~BR-29)
├── SpeedConfig.java              # 难度初始速度与随分阶梯提速(数值待 QA-17)
├── MapCatalog.java               # 主题地图集:管道/雪山字符画数据(A 模块维护)
├── SaveConfig.java               # 存档与最高分文件路径
└── UiConfig.java                 # 窗口与场景尺寸
```

> 全部为 `public static final` 常量或静态数据/方法,类内按 BR 编号注释便于追踪;**待确认数值以 `[待确认:QA-xx]` 标注,定稿后只改本包**。

### 4.1 BoardConfig(棋盘几何)

```text
BoardConfig.java                  # 棋盘几何常量
├── ROWS = 20                     # 行数
├── COLS = 20                     # 列数(棋盘 20×20 格)
├── CELL_SIZE_PX = 25             # 单格 25×25 px(棋盘绘制区 500×500 px,HUD 另占表头区)
└── snakeInit()                   # 开局蛇身初始坐标序列(默认棋盘中央 3 节、向右)
```

| 常量/方法 | 值/说明 | 依据 |
| ---- | ---- | ---- |
| `ROWS = 20`、`COLS = 20` | 棋盘 20×20 格 | BR 棋盘规格 |
| `CELL_SIZE_PX = 25` | 单格 25×25 px(棋盘绘制区 500×500 px,HUD 另占表头区) | 界面设计 |
| `snakeInit()` | 开局蛇身初始坐标序列(默认棋盘中央 3 节、向右) | 开局约定 |

### 4.2 BeanConfig(豆子调度与数值)

```text
BeanConfig.java                   # 豆子数值与调度参数(核心配置,BR-20~BR-29)
├── MAX_ON_BOARD = 3              # 场上豆子总数上限(含小豆子;非小豆子同时最多 2 颗)(BR-28)
├── SMALL_ON_BOARD = 1            # 小豆子场上恒 1 颗:开局 1 颗、被吃立补,独立于非小豆生成(BR-25)
├── POSITIVE_LIFETIME_MS = 5_000  # 正面豆(减负金豆/大豆子)在场 5 秒未吃消失(BR-28)
├── NEGATIVE_LIFETIME_MS = 3_000  # 负面豆(毒豆子/大毒豆子)在场 3 秒未吃消失(BR-28)
├── REFILL_DELAY_MS = 3_000       # 非小豆子被吃/超时消失后延迟 3 秒补刷 1 颗(BR-25)
├── DEBUFF_MS = 2_000             # 大毒豆方向颠倒持续 2 秒(再吃重置)
├── DEBUFF_SPAWN_FREE_CELL_LIMIT = 10  # 空格 < 10 时不刷 debuff 类豆(毒/大毒),仅刷正面(BR-25)
├── 正面池权重 1:1                # 非小豆正面池:减负金豆:大豆子(BR-29)
├── 负面池权重 1:3                # 非小豆负面池:毒豆子:大毒豆子(BR-29)
├── POSITIVE_TO_NEGATIVE = 3:1    # 正/负面池全局比例(客户 9.10 定稿,BR-29)
├── scoreOf(BeanType)             # 分值表:小+1/大+6/金+6/毒-6/大毒-10
├── tailEffectOf(BeanType)        # 蛇身效果表:+1 节 / 消除 2~3 节(金)/ 不变
└── lifetimeMs(BeanType)          # 在场时限查表:正面 5 s、负面 3 s、小豆子不限时(BR-28)
```

| 常量/方法 | 值/说明 | 依据 |
| ---- | ---- | ---- |
| `MAX_ON_BOARD = 3` | 场上豆子总数上限,含小豆子(小豆子 1 颗在场时,非小豆子同时最多 2 颗) | BR-28 |
| `SMALL_ON_BOARD = 1` | 小豆子场上恒定 1 颗(开局 1 颗、被吃立补;独立于非小豆子生成) | BR-25 |
| `POSITIVE_LIFETIME_MS = 5_000` | 正面豆(减负金豆/大豆子)在场 5 秒未吃消失 | BR-28 |
| `NEGATIVE_LIFETIME_MS = 3_000` | 负面豆(毒豆子/大毒豆子)在场 3 秒未吃消失 | BR-28 |
| `REFILL_DELAY_MS = 3_000` | 非小豆子被吃/超时消失后延迟 3 秒按 BR-29 概率补刷 1 颗 | BR-25 |
| `DEBUFF_MS = 2_000` | 大毒豆方向颠倒持续 2 秒(再吃重置) | BR 大毒效果 |
| `DEBUFF_SPAWN_FREE_CELL_LIMIT = 10` | 场上可刷新空格 < 10 时不补刷 debuff 类豆(毒/大毒),仅刷减负金豆与大豆子 | BR-25 |
| `scoreOf(BeanType)` | 分值表:小 +1、大 +6、金 +6、毒 -6、大毒 -10 | BR 分值 |
| `tailEffectOf(BeanType)` | 蛇身效果表(枚举):+1 节 / 消除 2~3 节(金,随机量由 BeanController 掷)/ 不变 | BR 蛇身变化 |
| 正面池权重 | 非小豆正面池 减负金豆:大豆子 = 1:1 | BR-29 |
| 负面池权重 | 非小豆负面池 毒豆子:大毒豆子 = 1:3 | BR-29 |
| `POSITIVE_TO_NEGATIVE = 3:1` | 正/负面池全局比例(客户 9.10 定稿;原"2:1 占位"废止) | BR-29 / QA-19 |
| `lifetimeMs(BeanType)` | 在场时限查表:正面 5 s、负面 3 s、小豆子不限时(直至被吃) | BR-28 |

### 4.3 SpeedConfig(难度与提速)

```text
SpeedConfig.java                  # 难度与提速参数
├── initIntervalMs(Difficulty)    # 难度 → 每格毫秒(低慢/中中/高快)[待确认:QA-17,占位]
├── tierFor(int score)            # 阶梯提速档位表:分数阈值 → 每格毫秒,只升不降 [待确认:QA-17]
└── MIN_INTERVAL_MS               # 速度上限(最快档位毫秒下限)[待确认:QA-17]
```

| 常量/方法 | 说明 | 依据 |
| ---- | ---- | ---- |
| `initIntervalMs(Difficulty)` | 难度 → 每格毫秒(低慢、中中、高快);数值开发期占位 **[待确认:QA-17]** | 难度规则 |
| `tierFor(int score)` | 随分数阶梯提速档位表(分数阈值 → 每格毫秒,只升不降);数值占位 **[待确认:QA-17]** | 提速规则 |
| `MIN_INTERVAL_MS` | 速度上限(最慢→最快区间下限);占位 **[待确认:QA-17]** | 提速上限 |

### 4.4 MapCatalog(地图集与障碍数据)

```text
MapCatalog.java                   # 主题地图集(A 模块维护地图布局数据)
├── defaultMaps()                 # 返回主题地图列表:管道/雪山(字符画 '#'=障碍、'.'=空,20×20)
└── byId(String)                  # 按 id 取图,取不到抛 IllegalArgumentException
```

| 方法 | 说明 | 依据 |
| ---- | ---- | ---- |
| `defaultMaps()` | 返回主题地图列表:管道、雪山(每图 20×20,字符画 `#`=障碍、`.`=空) | 主题地图 |
| `byId(String)` | 按 id 取图(取不到抛 IllegalArgumentException) | 选择页/存档 |

约定:地图字符画 20 行 × 20 列;**障碍不占四周边界通道**(依据需求地图规则);数据由 A 模块(地图设计)维护,碰撞判定只读消费。

### 4.5 SaveConfig(存档路径)

```text
SaveConfig.java                   # 本地文件路径配置(B 模块维护)
└── 存档/最高分文件名与目录        # 默认用户目录隐藏子目录,避免打包后不可写
```

| 内容 | 说明 |
| ---- | ---- |
| 存档/最高分文件名与目录 | 默认用户目录隐藏子目录,避免打包后不可写;单存档、最高分按地图 |

### 4.6 UiConfig(窗口)

```text
UiConfig.java                     # 窗口与场景尺寸(A 模块维护)
└── WINDOW_W/H、MIN_W/H           # 主舞台尺寸与最小尺寸(字体/配色等视觉值一律走 CSS)
```

| 内容 | 说明 |
| ---- | ---- |
| 窗口尺寸、场景最小尺寸等 | 字体/配色等视觉值一律走 CSS,不在此重复(依据界面设计) |

## 5. model(纯数据与状态)

```text
model/                            # 纯数据与状态,不含决策逻辑,不 import javafx(三模块共享)
├── Point.java                    # 不可变棋盘坐标
├── Direction.java                # 方向枚举:向量/禁掉头/颠倒映射
├── Board.java                    # 棋盘:界内/穿墙环化/障碍/空格枚举
├── GameMap.java                  # 主题地图与障碍集
├── Snake.java                    # 蛇链(Deque):前进/增长/消除/复制
├── BeanType.java                 # 豆类别枚举:小/大/金/毒/大毒,类别判定
├── Bean.java                     # 场上豆实例:类型/位置/出生时刻
├── Difficulty.java               # 难度枚举:低/中/高
├── GamePhase.java                # 状态机枚举:待开始/运行/暂停/结束
├── GameOverReason.java           # 结束原因枚举(结算文案依据)
├── GameOptions.java              # 主界面选择保持(难度+地图)
├── HighScoreTable.java           # 最高分表(按地图,不区分难度)
├── SaveData.java                 # 存档 DTO
├── ReadOnlyGameState.java        # GameState 的只读视图接口(供 view 渲染,防呆)
└── GameState.java                # 本局聚合根(implements ReadOnlyGameState;controller 唯一修改方)
```

> 原则:**不 import 任何 javafx 包;不含"何时刷豆/何时结束"类决策逻辑**;方法只做自身状态的查询与最小变换。所有类可被 JUnit 直测。

### 5.1 Point(不可变坐标)

```text
Point.java                        # 不可变棋盘坐标(基础类型)
└── equals/hashCode/toString      # 值比较与打印(row,col)
```

| 方法 | 作用 |
| ---- | ---- |
| `equals/hashCode/toString` | 不可变棋盘坐标的值比较与打印 | 

### 5.2 Direction(方向枚举)

```text
Direction.java                    # 方向枚举(UP/DOWN/LEFT/RIGHT)
├── dx() / dy()                   # 移动向量分量(与行/列坐标系一致)
├── isOpposite(Direction)         # 是否反向 → 禁止掉头判定
└── inverted()                    # debuff 颠倒映射(UP↔DOWN、LEFT↔RIGHT)
```

| 方法 | 作用 |
| ---- | ---- |
| `dx()`、`dy()` | 向量分量(移动/转向) |
| `isOpposite(Direction)` | 禁止掉头判定(例如右行时 LEFT 为反向) |
| `inverted()` | debuff 颠倒映射(UP↔DOWN、LEFT↔RIGHT) |

### 5.3 Board(棋盘)

```text
Board.java                        # 棋盘几何与占位查询
├── inside(Point)                 # 坐标是否界内
├── wrap(Point)                   # 穿墙环化:越界映射到对侧(依据穿墙规则)
├── obstacleAt(Point)             # 该格是否为障碍
├── freeCells(Set<Point>)         # 可刷新空格列表(排除障碍与传入占用)
└── countFree(Set<Point>)         # 可刷新空格数(空格 < 10 判定依据)
```

| 方法 | 作用 |
| ---- | ---- |
| `inside(Point)` | 坐标是否在界内 |
| `wrap(Point)` | 穿墙环化:越界坐标映射对侧对应位置 |
| `obstacleAt(Point)` | 该格是否为障碍 |
| `freeCells(Set<Point> occupied)` | 可刷新空格(排除障碍与占用),供豆子定位 |
| `countFree(Set<Point>)` | 可刷新空格计数(`< 10` 判定) |

### 5.4 GameMap(主题地图)

```text
GameMap.java                      # 地图数据(管道/雪山)
├── id / displayName              # 标识与展示名(如 "pipe" / "管道",供界面与存档引用)
├── obstacleAt(Point)             # 该格是否为障碍
└── fromCharMap(List<String>)     # 工厂:字符画 → 障碍集(供 MapCatalog 调用)
```

| 字段/方法 | 作用 |
| ---- | ---- |
| `id`、`displayName` | 地图标识与展示名(管道/雪山);displayName 供界面/记录页展示 |
| `obstacleAt(Point)` | 该格是否为障碍 |
| `fromCharMap(List<String>)` | 字符画 → 障碍集工厂(供 MapCatalog) |

### 5.5 Snake(蛇链)

```text
Snake.java                        # 蛇链,内部 Deque<Point>(头在前)
├── head()                        # 头坐标
├── length()                      # 当前节数
├── contains(Point)               # 蛇身是否占据该格(撞自身判定)
├── step(Point, boolean grow)     # 头前进;grow=false 去尾(普通走格),grow=true 保尾(长 1 节)
├── shrink(int n)                 # 从尾消除 n 节(金豆效果;下限 1 节由调用方保证)
├── body()                        # 身体坐标只读快照(渲染/存档)
└── copy()                        # 深拷贝(存档快照)
```

| 方法 | 作用 |
| ---- | ---- |
| `head()` | 头坐标(移动基准) |
| `length()` | 蛇身节数 |
| `contains(Point)` | 撞自身判定 |
| `step(Point newHead, boolean grow)` | 头前进:grow=false 去尾(=普通走格),grow=true 保留尾(=长一节) |
| `shrink(int n)` | 从尾消除 n 节(金豆效果,下限 1 节由调用方保证) |
| `body()` | 只读快照(渲染/存档) |
| `copy()` | 深拷贝(存档快照) |

### 5.6 BeanType(豆类别枚举)

```text
BeanType.java                     # 豆类别(小/大/金/毒/大毒);分值在 config,此处只判类别
├── isSmall()                     # 是否小豆子(独立生成/不限时语义判定)
├── isDebuff()                    # 是否触发方向颠倒(仅 BIG_POISON)
└── isPositive()                  # 是否正面豆(减负金豆/大豆子)
```

| 方法 | 作用 |
| ---- | ---- |
| `isSmall()` | 是否小豆子(独立生成/不限时语义判定) |
| `isDebuff()` | 是否触发方向颠倒(仅 BIG_POISON) |
| `isPositive()` | 是否正面豆(减负金豆/大豆子) |

> 注:原 `mutualGroup()`(大/金互斥判定)随 PRD v1.7 取消"大豆子与减负金豆互斥"规则(QA-20)一并删除;非小豆抽取不再剔除冲突项。

### 5.7 Bean(场上豆实例)

```text
Bean.java                         # 场上豆实例
├── type / pos / bornMs           # 类别 / 位置 / 出生时刻(游戏时间)
└── remainingMs(long nowMs)       # 非小豆子在场剩余寿命(负=已超时;小豆子不限时,不参与时限判定),供时限判定与存档折算
```

| 字段/方法 | 作用 |
| ---- | ---- |
| `type`、`pos`、`bornMs` | 类别/位置/产生时刻(游戏时间戳) |
| `remainingMs(long nowMs)` | 非小豆子在场剩余寿命(负=已超时);存档折算剩余的依据;小豆子不限时,不参与时限判定 |

### 5.8 Difficulty(难度枚举)

```text
Difficulty.java                   # EASY/NORMAL/HARD(低/中/高)
```

无方法;作为 config 速度表与 GameOptions 的键。

### 5.9 GamePhase(状态机枚举)

```text
GamePhase.java                    # READY(待开始)/RUNNING/PAUSED/FINISHED
```

无方法;由 GameController 迁移,view 按 phase 切换界面层。

### 5.10 GameOverReason(结束原因枚举)

```text
GameOverReason.java               # SELF_COLLISION/OBSTACLE_HIT/SCORE_NEGATIVE/CLEARED/ABANDONED
```

无方法;结算画面原因文案由 view 映射;`CLEARED`(通关)枚举预留,判定待 QA-15。

### 5.11 GameOptions(主界面选择保持)

```text
GameOptions.java                  # 主界面选择的难度与地图(跨页面、跨局保持)
├── getDifficulty() / setDifficulty(...)
├── getMap() / setMap(...)        # 默认:低难度 + MapCatalog 第一张图
└── (读写方法)
```

| 字段 | 默认 | 作用 |
| ---- | ---- | ---- |
| `difficulty` | EASY(低) | 难度选择保持(FP-02) |
| `map` | 第一张地图 | 地图选择保持(FP-02) |

### 5.12 HighScoreTable(最高分表)

```text
HighScoreTable.java               # 按地图统计的最高分(mapId → score)
├── scoreOf(String mapId)         # 查询某图最高分(无记录返回 0)
└── putIfHigher(String, int)      # 更高则覆盖并返回 true(新纪录判定)
```

| 方法 | 作用 |
| ---- | ---- |
| `scoreOf(mapId)` | 某图最高分查询(无记录返回 0) |
| `putIfHigher(mapId, score): boolean` | 仅更高时覆盖,返回是否新纪录(最高分按地图、不区分难度) |

### 5.13 SaveData(存档 DTO)

```text
SaveData.java                     # 存档数据载体(纯 DTO,编解码由 util.TextCodec 完成)
├── mapId / difficulty / score    # 局配置与得分
├── body: List<Point>             # 蛇身坐标序列
├── beans: {type, pos, remainingMs}  # 豆子存"剩余寿命"而非出生时刻,恢复时折算 bornMs
├── debuffRemainingMs             # 方向颠倒剩余
├── intervalMs                    # 当前每格耗时(速度档)
└── gameTimeMs                    # 存档时游戏时间
```

> 存档字段默认按上表, **[待确认:QA-11]**;豆子补刷"进行中"的延迟计划默认不入档(恢复后按当前豆子数量自然继续);若 QA-11 要求完整恢复再扩展字段并同步 TextCodec。

### 5.14 ReadOnlyGameState(只读视图接口)

```text
ReadOnlyGameState.java            # GameState 的只读接口(view 在类型层面无法调用变更方法)
├── score() / snake() / beans() / map()   # 局面只读句柄
├── phase() / overReason()                # 状态与结束原因
├── debuffRemainingMs() / intervalMs() / gameTimeMs()  # 倒计时与时间读数
└── (仅含读方法;变更方法只存在于 GameState)          # 由 GameState implements
```

| 说明 |
| ---- |
| 接口只暴露读方法;`GameController.state()` 返回本接口,view/测试一律以只读类型持有 GameState;`GameState implements ReadOnlyGameState` |

### 5.15 GameState(本局聚合根,controller 唯一修改方)

```text
GameState.java                    # 本局全部运行状态;implements ReadOnlyGameState(读方法见 §5.14);controller 唯一变更方
├── phase / overReason            # 状态机与结束原因
├── difficulty / map              # 本局难度与地图(来自 GameOptions)
├── score / snake                 # 得分与蛇
├── beans: List<Bean>             # 场上豆(总数 ≤ 3 颗,含小豆子 1 颗/非小豆最多 2 颗;对外只读)
├── debuffRemainingMs             # 方向颠倒剩余(0=无)
├── intervalMs                    # 当前每格耗时(难度初始 → 提速下调 → 上限)
├── gameTimeMs                    # 游戏时间累计(RUNNING 才推进 → 暂停冻结的根源)
├── 读方法(score()/snake()/beans()/…)  # 经 ReadOnlyGameState 接口对外暴露(§5.14)
├── setPhase(GamePhase)           # 状态迁移(controller 专用)
├── finish(GameOverReason)        # 终局:置 FINISHED 与原因
├── addScore(int)                 # 结算分数;完成后 < 0 返回负值由 controller 判负
├── addBean(Bean) / removeBean(Bean)  # 场上豆增删(BeanController 专用)
├── setDebuffRemainingMs(long)    # debuff 倒计时设定
├── setIntervalMs(long)           # 提速后更新每格耗时
└── advanceGameTime(long delta)   # 累计游戏时间(仅 RUNNING)
```

| 字段 | 说明 |
| ---- | ---- |
| `phase`、`overReason` | 状态机与结束原因 |
| `difficulty`、`map` | 本局难度与地图(来自 GameOptions) |
| `score`、`snake` | 得分与蛇 |
| `beans: List<Bean>` | 场上豆(总数 ≤ 3 颗,含小豆子 1 颗;view 只读,controller 增删) |
| `debuffRemainingMs` | 方向颠倒剩余(0 = 无) |
| `intervalMs` | 当前每格耗时(难度初始值 → 提速下调 → 上限) |
| `gameTimeMs` | 游戏时间累计(RUNNING 才推进;暂停冻结的根源) |

| 方法(读,经 ReadOnlyGameState 对外暴露) | 方法(变更,仅 controller 可调) |
| ---- | ---- |
| `score()`、`snake()`、`beans()`、`map()`、`phase()`、`overReason()`、`debuffRemainingMs()`、`intervalMs()`、`gameTimeMs()` | `setPhase(...)`、`finish(GameOverReason)`、`addScore(int)`(完成后若 < 0 返回负值由 controller 判负)、`addBean(Bean)`、`removeBean(Bean)`、`setDebuffRemainingMs(long)`、`setIntervalMs(long)`、`advanceGameTime(long delta)` |

## 6. util(无状态工具与基础设施)

```text
util/                             # 无状态工具与基础设施(可被任何业务包调用)
├── GameClock.java                # 时间源接口 nowMs()
├── SystemClock.java              # 生产实现:系统当前毫秒
├── SimulatedClock.java           # 测试实现:手动拨时间(AC 定时断言)
├── WeightedPicker.java           # 按权重随机抽取(注入 Random,可固定种子)
├── Store.java                    # 存储接口(读写/删除/存在;注入点)
├── LocalStore.java               # Store 的文件系统实现(路径来自 SaveConfig)
├── InMemoryStore.java            # Store 的内存实现(仅测试用)
└── TextCodec.java                # 存档/最高分 ↔ 文本编解码(UTF-8 键值行)
```

> **不包含任何 Timer/Thread 倒计时器**:全部"时长"由 controller 以注入时钟被动判定,保证暂停冻结与可测性(见 §10)。

### 6.1 GameClock(时间源接口)

```text
GameClock.java                    # 时间源抽象(接口)
└── nowMs(): long                 # 返回当前时间毫秒
```

### 6.2 SystemClock(生产时钟)

```text
SystemClock.java                  # 实现 GameClock(生产用)
└── nowMs()                       # System.currentTimeMillis()
```

### 6.3 SimulatedClock(测试时钟)

```text
SimulatedClock.java               # 实现 GameClock(测试用)
├── nowMs()                       # 返回虚拟当前时间
└── advanceMs(long)               # 手动拨快虚拟时间 → AC-17/AC-18 定时断言
```

### 6.4 WeightedPicker(加权随机)

```text
WeightedPicker.java               # 按比例随机抽取
├── 构造(Random)                  # 注入随机源;测试传固定种子 Random(0) 可复现
├── <T> pick(List<T>, List<Integer> weights)  # 按权重取一(豆类型抽取)
└── nextInt(int bound)            # 随机整数(金豆消除 2~3 节等)
```

| 方法 | 作用 |
| ---- | ---- |
| 构造注入 `Random` | 测试可固定种子(AC-18 比例统计可复现) |
| `<T> pick(List<T>, List<Integer> weights)` | 按比例抽豆类型 |
| `nextInt(int)` | 金豆消除 2~3 节等随机决策 |

### 6.5 Store(存储接口)

```text
Store.java                        # 存储抽象(接口):文件/内存两个实现
├── saveText(String file, String content)   # 写入(目录/文件名由调用方给定)
├── loadText(String): Optional<String>      # 读取(文件缺失返回 empty)
├── delete(String)                # 删除
└── exists(String): boolean       # 是否存在
```

> 判定/组装配逻辑在 SaveController(规则层),本接口只做原始存取;生产注入 `LocalStore`,测试注入 `InMemoryStore`。

### 6.6 LocalStore(存储文件实现)

```text
LocalStore.java                   # Store 的文件系统实现(生产用;B 模块维护)
└── 实现 saveText/loadText/delete/exists  # 目录与文件名来自 SaveConfig(默认用户目录隐藏子目录)
```

### 6.7 InMemoryStore(存储测试实现)

```text
InMemoryStore.java                # Store 的内存实现(仅测试用,不进生产装配)
└── 实现 saveText/loadText/delete/exists  # 内部 Map 存储;SaveController 单测不碰磁盘
```

### 6.8 TextCodec(文本编解码)

```text
TextCodec.java                    # SaveData/HighScoreTable ↔ 文本
├── encodeSave(SaveData): String / decodeSave(String): SaveData
└── encodeScores(HighScoreTable) / decodeScores(String): HighScoreTable
```

| 方法 | 作用 |
| ---- | ---- |
| `encodeSave` / `decodeSave` | 存档文本编解码(UTF-8 键值行),字段随 SaveData 定稿 |
| `encodeScores` / `decodeScores` | 最高分表文本编解码 |

## 7. controller(规则引擎与决策 = MVC Controller)

```text
controller/                       # 规则引擎与决策(= MVC 的 Controller);全部不 import javafx,可 JUnit 直测
├── GameController.java           # 主循环与状态机:推进/碰撞/结算/暂停/提速(C 模块)
├── InputController.java          # 输入判定:0.15s 窗口/禁掉头/方向颠倒(C 模块)
├── BeanController.java           # 豆子调度状态机:小豆独立生成/非小豆延迟补刷(B 模块,最复杂)
├── SaveController.java           # 存档组装配与最高分判定(B 模块)
└── GameEvents.java               # 事件接口(controller 触发,view 实现,定义于本包)
```

### 7.1 GameController(主循环与状态机)

```text
GameController.java               # 游戏主控制器(C 模块);view 每帧只调 tick()
├── nextMoveAtMs                  # 下一移动节拍的游戏时刻(初始 0,之后 += intervalMs)
├── lastRealMs                    # 上帧真实时钟,用于累计游戏时间
├── newGame(GameOptions)          # 建局:难度取 intervalMs/蛇居中/BeanController.spawnInitial();phase=READY
├── tick(long realNowMs)          # 唯一推进入口:非 RUNNING 直接返回(=暂停/待开始/结束全冻结)
│                                 #   → 累计 gameTimeMs → 到节拍 stepOnce() → debuff 递减 → BeanController.tick → maybeSpeedUp
├── queueInput(Direction, long)   # 转发 InputController(0.15 s 窗口在接收时判定)
├── pause() / resume()            # RUNNING ↔ PAUSED(时间冻结语义见 tick)
├── restart()                     # 结算按 R:以同难度/地图重开
├── finish(GameOverReason)        # phase=FINISHED、清 debuff、触发 onGameOver(stepOnce 内部或外部调用)
├── state(): ReadOnlyGameState   # view 渲染只读句柄(类型层面只读)
└── registerEvents(GameEvents)    # view 订阅事件(A 不猜状态,diff 反模式禁止)
```

| 字段 | 说明 |
| ---- | ---- |
| `nextMoveAtMs` | 下一移动节拍的游戏时刻(初始 0,之后 += intervalMs) |
| `lastRealMs` | 上帧真实时钟,用于累计游戏时间 |

| 方法 | 作用 |
| ---- | ---- |
| `newGame(GameOptions)` | 建局:按难度取初始 intervalMs;蛇置中;`BeanController.spawnInitial()`;phase=READY;通知事件 |
| `tick(long realNowMs)` | **唯一推进入口**(view 每帧调用):非 RUNNING 直接返回(暂停/待开始/结束冻结);累计 `gameTimeMs += Δ`;到节拍点执行 `stepOnce()`;debuff 递减到 0;`BeanController.tick(gameTime)`;得分跨阈值则 `maybeSpeedUp()` |
| `queueInput(Direction, long realMs)` | 转发 InputController(0.15 s 窗口判定在接收时完成) |
| `pause() / resume()` | RUNNING↔PAUSED(游戏时间不累计 → 豆时限/倒计时/节拍全冻结);事件通知 |
| `restart()` | 结算画面按 R:以同难度/地图重开(newGame(当前 options)) |
| `finish(GameOverReason)` | phase=FINISHED、清 debuff、事件 `onGameOver`;由 stepOnce 内部触发(SELF_COLLISION/OBSTACLE_HIT/SCORE_NEGATIVE)或外部(ABANDONED) |
| `state(): ReadOnlyGameState` | view 渲染只读句柄(类型层面只读,见 §5.14) |
| `registerEvents(GameEvents)` | view 订阅事件(A 不猜状态,diff 反模式禁止) |

**私有流程 `stepOnce()`**:取 `InputController.resolveTurn(当前头方向, debuff 有效)` → `board.wrap(下一格)` → 判定:障碍 → finish(OBSTACLE_HIT);蛇身 → finish(SELF_COLLISION);豆子命中 → `applyBeanEffect(...)`(加分/长节或金豆消除 2~3 节/毒类扣分、大毒重置 debuff 2 秒并事件;扣分后 score<0 → finish(SCORE_NEGATIVE));未命中 → 普通走格(去尾)。吃豆与事件同时通知 `BeanController.onBeanEaten(bean, gameTime)`。

**私有流程 `maybeSpeedUp()`**:score 达下一档阈值 → `intervalMs` 下调(不高于 MIN_INTERVAL_MS),仅升档不回退。

### 7.2 InputController(输入判定)

```text
InputController.java              # 按键 → 合法转向(C 模块)
├── pendingDir                    # 待生效方向(一拍转向缓冲,不排队)
├── lastDir / lastAtMs            # 上次生效方向与其真实时刻
├── queueInput(Direction, long realMs)  # 距上次 < 0.15 s 视为连按:只更新方向不重计时;否则入缓冲
└── resolveTurn(Direction headDir, boolean inverted): Direction
                                  # 节拍点消费缓冲:与 headDir 相反 → 保持原向(禁掉头);inverted → 整体取反;记录 lastDir/lastAtMs
```

| 字段 | 说明 |
| ---- | ---- |
| `pendingDir` | 待生效方向(一拍的转向缓冲) |
| `lastDir`、`lastAtMs` | 上次生效方向与其真实时刻(0.15 s 窗口) |

| 方法 | 作用 |
| ---- | ---- |
| `queueInput(Direction raw, long realMs)` | 距上次 < 0.15 s 视为连按:仅更新方向不重计时;否则正常入缓冲(只存一拍,不排队) |
| `resolveTurn(Direction headDir, boolean inverted): Direction` | 节拍点消费缓冲:与 headDir 相反 → 保持原向(禁止掉头);`inverted` 时整体取反后再返回;记录 lastDir/lastAtMs |

### 7.3 BeanController(豆子生成/刷新/调度 —— 全场最复杂)

```text
BeanController.java               # 豆子调度状态机(B 模块);实现 BR-25/BR-28/BR-29(PRD v1.7)
├── refillDueAt: List<Long>       # 非小豆子补刷到期游戏时刻(被吃/超时各登记 1 个,可多个并存)
├── spawnInitial()                # 开局补满 3 颗:小豆子 1 颗 + 非小豆子 2 颗(按 BR-29 抽);逐颗落空白格;bornMs=0
├── spawnSmall()                  # 小豆子被吃后立即补位:随机空白格落 1 颗;场上恒 1 颗、不限时
├── onBeanEaten(Bean, long gameTime)  # 小豆被吃 → 立即 spawnSmall();非小豆被吃 → 登记 refillDueAt += gameTime+3000
├── tick(long gameTime)           # ① 非小豆寿命 ≤ 0(正面 5 s/负面 3 s)→ 移除并登记 refillDueAt += gameTime+3000
│                                 #   (小豆不限时跳过);② 到期计划逐一 spawnOne()
└── spawnOne()                    # 抽取 1 颗非小豆:先按正:负=3:1 定池(空格<10 时负面池被排除)→ 池内 金:大=1:1 或
                                  #   毒:大毒=1:3 抽;总数达 3 颗不新增;落位随机空白格(排除蛇身/障碍/已有豆)
```

| 字段 | 说明 |
| ---- | ---- |
| `refillDueAt: List<Long>` | 非小豆子补刷到期游戏时刻(被吃或超时消失各登记 1 个,3 秒后到期;可多个并存;未落地不入档,见 §5.13 注) |

| 方法 | 分支逻辑 | 依据 |
| ---- | ---- | ---- |
| `spawnInitial()` | 开局**立即补满 3 颗**:小豆子 1 颗(固定)+ 非小豆子 2 颗(按 BR-29 概率抽取);逐颗落位于空白格(排除蛇身/障碍/已有豆),`bornMs = gameTime(0)` | BR-28 开局 / QA-20 |
| `spawnSmall()` | 小豆子被吃后的立即补位:随机空白格落 1 颗(不与其他豆子重合);小豆子不限时、场上恒 1 颗,独立于非小豆子生成 | BR-25 ① |
| `onBeanEaten(Bean, long gameTime)` | 小豆子被吃 → **立即补 1 颗小豆子**(`spawnSmall()`);非小豆子被吃 → 登记 `refillDueAt += gameTime + 3000`(3 秒后按 BR-29 补刷 1 颗) | BR-25 ①② |
| `tick(long gameTime)` | ① 遍历场上非小豆子:寿命 ≤ 0(正面 5 s、负面 3 s;小豆子不限时跳过)→ 移除并登记 `refillDueAt += gameTime + 3000`;② 到期计划逐一 `spawnOne()` | BR-28 时限、BR-25 ② |
| `spawnOne()` | 抽取 1 颗非小豆子:先按正:负 = 3:1 定池(空格 < 10 时负面池被排除、仅正面池参与);池内按 金:大 = 1:1 或 毒:大毒 = 1:3 抽取;场上总数达 3 颗时不新增;落位随机空白格(排除蛇身/障碍/已有豆) | BR-25 ③/位置、BR-29 |

> 实现提示:补刷**不记忆原空位类别**(延迟空位一律按 BR-29 重新抽取),与 BR-25②"类别按 BR-29 抽取"一致;**大/金互斥自 v1.7 取消**(QA-20),抽取不再剔除冲突项。

### 7.4 SaveController(存档组装配与最高分)

```text
SaveController.java               # 存档/读档与最高分(B 模块);构造注入 Store(生产 LocalStore / 测试 InMemoryStore)
├── hasSave(): boolean            # 是否存在单存档
├── saveNow(GameState)            # 组装配:豆子折算剩余寿命/记 gameTimeMs → SaveData → TextCodec → LocalStore 写(覆盖旧档)
├── loadAndResume(): Optional<GameState>  # 读档重建:蛇身/豆子 bornMs = gameTimeMs − remainingMs;恢复为 PAUSED [默认,待 QA-11]
├── deleteSave()                  # 删除存档(终止游戏时)
├── highScoreOf(String mapId): int       # 记录页/结算页只读查询
└── recordScore(String mapId, int): boolean  # 高于现纪录则写入并返回 true("新纪录"依据)
```

| 方法 | 作用 |
| ---- | ---- |
| `hasSave(): boolean` | 是否存在单存档 |
| `saveNow(GameState)` | 组装配:豆子折算**剩余寿命**、记录 gameTimeMs 等 → SaveData → TextCodec → LocalStore 写入(覆盖旧档) |
| `loadAndResume(): Optional<GameState>` | 读取 → 重建 GameState(蛇身/豆子 bornMs = gameTimeMs − remainingMs;恢复为 PAUSED 由玩家继续 **[默认,UI 细节待 QA-11]**) |
| `deleteSave()` | 删除存档(如终止游戏时) |
| `highScoreOf(String mapId): int` | 记录页/结算页只读查询 |
| `recordScore(String mapId, int score): boolean` | 高于现纪录则写入并返回 true(结算画面"新纪录"依据) |

### 7.5 GameEvents(事件接口,定义于 controller,view 实现)

| 事件 | 触发方 | view 用途 |
| ---- | ---- | ---- |
| `onBeanEaten(BeanType, Point)` | GameController(效果结算后) | 吃豆爆点/飘分特效 |
| `onBeanRefilled(Bean)` | BeanController(tick 补刷成功后) | 新豆出现动画(可选) |
| `onDebuffChanged(long remainingMs)` | GameController | HUD 颠倒横幅/倒计时 |
| `onGameOver(GameOverReason)` | GameController(finish) | 结算画面(最终分/最高分/原因文案) |
| `onPhaseChanged(GamePhase)` | GameController | 暂停层显隐等 UI 联动 |

## 8. view(JavaFX 界面与美术)

```text
view/                             # 全部 JavaFX 界面(A 模块);只收事件转发 + 只读渲染,规则零行
├── PageRouter.java               # 单 Stage 页面切换(场景路由)
├── MainMenuView.java             # 主界面:标题 + 五按钮
├── GameSetupView.java            # 难度/地图选择页(高亮保持 + 地图缩略预览)
├── RecordsView.java              # 记录页(各地图最高分)
├── GameView.java                 # 游戏主场景:HUD + Canvas 棋盘 + 弹层 + 渲染循环
├── Palette.java                  # 主题配色表(换主题只换色值,绘制代码一份)
├── widgets/                      # 可复用控件:PrimaryButton/Toast/ConfirmDialog
└── fx/                           # 特效:BeanBurst/ScorePopup/ScreenFlash/DirectionBanner
```

> 页面类命名 `*View`;渲染方案:游戏棋盘用**单 `Canvas` 每帧重绘**(AnimationTimer),不维护 400+ 网格节点。规则逻辑零行:只收事件转发 + 只读渲染。

### 8.1 PageRouter(页面路由)

```text
PageRouter.java                   # 页面切换管理
├── showMenu() / showSetup() / showRecords()
├── showGame()                    # 进入游戏场景(启动 GameView 渲染循环)
└── (持有各 View 与 controller 引用)
```

### 8.2 MainMenuView(主界面)

```text
MainMenuView.java                 # 主界面
├── 标题 + 五按钮                  # 开始新游戏 / 继续游戏 / 游戏难度 / 地图 / 记录
└── 继续游戏逻辑                   # 无存档 → 弹"无保存记录"提示(hasSave 判定)
```

### 8.3 GameSetupView(难度/地图选择)

```text
GameSetupView.java                # 难度与地图选择页
├── 难度三卡(低/中/高)             # 选中高亮
├── 地图两卡(管道/雪山)            # 含迷你棋盘缩略预览(Canvas 复用渲染代码)
├── 返回键(右上角)                 # 回主界面;选中保持于 GameOptions
└── 开始                         # 调 GameController.newGame(GameOptions)
```

### 8.4 RecordsView(记录页)

```text
RecordsView.java                  # 历史记录页
├── 各地图最高分列表               # 数据只读自 SaveController.highScoreOf;空态提示"暂无记录"
└── 返回键(右上角)
```

### 8.5 GameView(游戏主场景,工作量最大)

```text
GameView.java                     # 游戏界面:三层 = HUD / Canvas 棋盘 / 特效与弹层
├── startRenderLoop()             # AnimationTimer 每帧 → GameController.tick(now) → render(state 快照)
├── render(GameState)             # 单 Canvas 重绘:棋盘格/障碍/五类豆/蛇(主题配色取自 Palette)
├── HUD                           # 得分、debuff 倒计时徽章、右上角暂停按钮
├── 暂停弹层                       # 继续 / 保存游戏(→SaveController.saveNow + Toast)/ 终止游戏
├── 结算层                         # 结束原因文案 + 最终得分 + 该图最高分 + 新纪录表现;R 重开 / Esc 回主界面
├── 按键转发                       # 方向 → GameController.queueInput;空格 → pause/resume;R/Esc
└── 实现 GameEvents                # onBeanEaten/onDebuffChanged/onGameOver/onPhaseChanged → 特效与弹层
```

### 8.6 Palette(主题配色)

```text
Palette.java                      # 主题调色板(A 模块内部)
└── 按主题返回配色表               # 棋盘格纹/障碍/蛇(头尾渐变)/五类豆色值;换主题只换色值
```

### 8.7 widgets(可复用控件)

| 类 | 用途 |
| ---- | ---- |
| `PrimaryButton` | 主按钮统一样式与字号 |
| `Toast` | 轻提示(保存成功/新纪录等,右下角淡入淡出) |
| `ConfirmDialog` | 确认弹窗通用容器 |

### 8.8 fx(特效与动画)

| 类 | 用途 |
| ---- | ---- |
| `BeanBurst` | 吃豆爆点动画(播放于吃豆位置) |
| `ScorePopup` | 得分飘字 |
| `ScreenFlash` | 吃毒/死亡红闪 |
| `DirectionBanner` | 方向颠倒横幅 + 实时倒计时 |

> 豆子视觉必须**形状+颜色双通道可辨**(小豆红点 / 大豆橙大圆 / 金豆金星 / 毒豆紫×纹 / 大毒豆暗紫大圆×纹);蛇头身区分、节间留隙、头尾渐变;障碍不占边界(依据地图规则)。**特效与提示不得影响规则正确性**,暂停期间整棵场景树冻结(不播动画)。

### 8.9 资源目录(resources)

| 资源目录 | 内容 |
| ---- | ---- |
| `resources/css/main.css` | 全局:字体族/字号档位/按钮/弹窗/HUD 公共样式 |
| `resources/css/pipe-theme.css`、`snow-theme.css` | 管道/雪山主题:棋盘格纹、障碍、蛇、豆视觉变量 |
| `resources/fonts/`、`resources/images/` | 可选字体与素材 |

## 9. 跨包协作:关键时序

### 9.1 启动与开新局

```
MainApp.start → 装配 SystemClock + 四 Controller → PageRouter.showMenu
主界面「开始新游戏」→ GameOptions(保持的选择)→ GameController.newGame → BeanController.spawnInitial
→ PageRouter.showGame → GameView 注册 GameEvents 并启动渲染循环
```

### 9.2 游戏中每帧(60 fps 渲染 / 每格毫秒级移动)

```
GameView.AnimationTimer ──tick(realNowMs)──▶ GameController
   ├─ gameTimeMs += Δ(仅 RUNNING;暂停/待开始/结束 → 直接返回 = 全冻结)
   ├─ 到节拍 → stepOnce(InputController.resolveTurn → 穿墙 → 障碍/自身/豆子判定)
   ├─ 吃豆 → 效果结算 + 事件 onBeanEaten + BeanController.onBeanEaten(小豆立补 / 非小豆登记 3 秒补刷)
   ├─ debuff 递减 / maybeSpeedUp / BeanController.tick(非小豆超时消失·延迟补刷到期;小豆不限时)
   └─ 返回后 GameView 读 state() 快照重绘 Canvas(蛇/豆/障碍)
```

### 9.3 暂停 / 保存 / 读档 / 结算

- 空格或暂停按钮 → `pause()`:之后 tick 不再累计游戏时间,豆在场计时、debuff、移动节拍**同时冻结**(BR-03 语义);
- 「保存游戏」→ `SaveController.saveNow(state)` → Toast 提示;
- 主界面「继续游戏」→ hasSave? 无 → "无保存记录"弹窗;有 → `loadAndResume()` → 进入 GameView(恢复为 PAUSED);
- `finish(reason)` → onGameOver → 结算层显示最终得分与当前地图最高分;破纪录 → `recordScore` 返回 true → 新纪录表现;R = `restart()`;Esc = 回主界面。

## 10. 时间模型与可测性(实现红线)

1. **单一时钟**:生产 = `SystemClock`;测试 = `SimulatedClock`(`advanceMs` 拨时间);
2. **游戏时间累计在 GameController**:仅 RUNNING 累加 → 暂停冻结零特判,豆/倒计时/节拍全部以游戏时间为基准;
3. **0.15 s 按键窗口用真实时间**(InputController),暂停时 UI 不转发按键,无冲突;
4. **测试断言**:AC-17"毒豆子满 3 s 消失、正面豆满 5 s 消失、小豆子不消失"= 建局 → 拨对应时长 → tick → 断言移除与延迟补刷计划生效;AC-18 比例统计 = 固定 Random 种子统计**非小豆子**生成频次(金:大 ≈ 1:1、毒:大毒 ≈ 1:3、正:负 ≈ 3:1;小豆子独立生成、不参与比例统计,统计口径按 BR-25/BR-29);
5. controller/model/util 零 javafx import,整包可被 JUnit 直测;view 仅做冒烟/人工走查。

## 11. 分工归属(9.9 起:按包固定负责人制)

分工排班以 **README「分工与协作机制」为唯一来源**,本节仅给出架构层面的归属映射。9.9 起按**包固定负责至交付**:`config`/`model`/`util`/`controller`/`view` 各 1 名负责人(代号 A~E),另 1 名**专职 PM**(代号 F,不承担包开发,负责排期、接口契约协调、评审合入与互测验收组织)。**姓名由晨会确认后填入 README 分工表**,本节以代号记。

| 代号 | 负责包 | 架构层面职责要点 |
| ---- | ---- | ---- |
| A | `config` | 棋盘几何/豆子数值/速度/地图字符画/存档路径/窗口参数;客户数值变更只改本包,相关参数 AC 自测 |
| B | `model` | 纯数据模型牵头起草(共享区);字段与只读边界(`ReadOnlyGameState`)先行定稿,供各包引用 |
| C | `util` | 无状态工具:GameClock(SystemClock/SimulatedClock)、WeightedPicker、Store 接口与 LocalStore/InMemoryStore、TextCodec |
| D | `controller` | 规则引擎与决策(工作量最大):GameController(主循环)、InputController(输入判定)、BeanController(豆子调度)、SaveController(存档/最高分)、GameEvents |
| E | `view` | 全部 JavaFX 界面与美术:PageRouter 与各 *View、Canvas 渲染与 HUD、widgets/fx 特效、CSS 双主题与字体资源 |
| F | 专职 PM(无包) | 晨会按 FP 清单(PRD 6.1.2)排当日任务并落包(各包 0~2 个);跨包接口契约协调;code review 与合入 dev/main;组织包间互测执行 AC 与异常用例;更新开发日报与文档族 |

**包内功能域参考**(即 9.8 版 README/本文档的 A/B/C 分工表;9.9 起域字母**不再对应人员**,仅供理解"某类规则由哪些类承接"):

| 包/资源 | UI 美术域(原 A) | 静态规则域(原 B) | 动态规则域(原 C) |
| ---- | ---- | ---- | ---- |
| `config` | MapCatalog(地图布局数据)、UiConfig | BeanConfig | SpeedConfig |
| `model` | GameOptions(选择保持)、GameMap(展示名) | BeanType/Bean、HighScoreTable | Point/Direction/Snake/Board、GamePhase/GameOverReason、GameState 与只读接口 ReadOnlyGameState |
| `util` | — | WeightedPicker(抽取)、Store/LocalStore/InMemoryStore、TextCodec | GameClock、SystemClock |
| `controller` | — | BeanController、SaveController | GameController、InputController |
| `view` + resources | 全部 | —(只消费事件与只读接口) | —(只消费事件与只读接口) |
| `org.example` | 全体(F 专职 PM 协调装配) | | |

**跨模块接口契约(晨会先对齐,避免并行冲突)**:① model 各字段的读写边界(§5.15);② GameEvents 事件清单(§7.5);③ GameState 对 view 的只读视图(§7.1 state());④ 存档字段集合(§5.13,QA-11 定稿后冻结)。

## 12. 需求追踪速查(主要 BR/FP → 落点)

| 规则/功能点 | 主要落点 |
| ---- | ---- |
| 状态机与暂停冻结(BR-03 等) | GameController + GameState.gameTimeMs |
| 禁止掉头 / 0.15 s 按键 / 方向颠倒 | InputController.resolveTurn / queueInput |
| 穿墙 / 碰撞 / 结束判定 | Board.wrap + GameController.stepOnce |
| 豆子生成与刷新状态机(BR-25) | BeanController(spawnInitial/spawnSmall/onBeanEaten/tick/spawnOne) |
| 容量·在场时限(BR-28,小豆子恒 1 颗) | BeanConfig + Bean.remainingMs + BeanController.tick |
| 生成概率(BR-29:金:大=1:1、毒:大毒=1:3、正:负=3:1) | BeanConfig 权重 + WeightedPicker |
| 分值/蛇身效果/金豆消除 | BeanConfig.scoreOf/tailEffectOf + GameController.stepOnce |
| 难度与阶梯提速(FP-11/FP-12) | SpeedConfig + GameController.maybeSpeedUp |
| 存档与最高分(FP-13/FP-14) | SaveController + SaveData + LocalStore/TextCodec |
| 主题地图与障碍(FP-15) | MapCatalog + GameMap + Palette/CSS 主题 |
| 界面/HUD/弹窗/特效(FP-01/02/07/09/10 表现侧) | view 各 View + widgets + fx |

## 13. 待确认项与影响清单

| 编号 | 内容 | 当前默认 | 影响位置 |
| ---- | ---- | ---- | ---- |
| QA-11 | 存档内容字段 | 按 §5.13 集合,补刷计划不入档;读档恢复为暂停态 | SaveData、TextCodec、SaveController |
| QA-15 | 通关胜利条件与结算表现 | CLEARED 枚举已预留,判定与表现待答复后补 | GameController、GameOverReason、GameView |
| QA-17 | 难度初始速度与提速档位数值 | SpeedConfig 占位值 | 仅 config(SpeedConfig) |

> QA-19(正/负面豆比例 = 3:1)与 QA-20(开局补满 3 颗、大/金互斥取消)已由客户 9.10 答复并并入本文档 v1.4(§4.2/§7.3);QA-12 原答复中"小:金:大=3:1:1、毒:大毒=3:1、大/金互斥"自 PRD v1.7 起废止。
> 定稿流程:客户/PM 答复 → 只改 config 与上述影响类 → 同步本文档、README 与需求文档(如涉及 BR/AC)。

## 14. 变更记录

| 版本 | 日期 | 说明 |
| ---- | ---- | ---- |
| v1.4 | 2026-09-11 | 豆子生成与刷新规则同步 PRD v1.7(客户 9.10 补充):小豆子独立生成、总数上限含小豆子、生成概率 金:大=1:1/毒:大毒=1:3/正:负=3:1、大金互斥取消;§4.2/§5.6/§5.7/§5.15/§7.3/§9.2/§10/§12/§13 同步 |
| v1.3 | 2026-09-09 | 分工切换为按包固定负责人制(代号 A~E 包负责人 + F 专职 PM);§11 重写,原 A/B/C 域表降级为"包内功能域参考" |
| v1.2 | 2026-09-09 | 接口化修订:model 拆只读接口 ReadOnlyGameState;util 拆存储接口 Store(LocalStore + InMemoryStore) |
| v1.1 | 2026-09-09 | 格式重排:每包章首结构树 + 类与方法行式清单;原表格信息按类保留 |
| v1.0 | 2026-09-09 | 首版(内容见 §1 修订记录) |
