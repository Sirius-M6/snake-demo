# 团队协作上传文件清单

> 本文件说明 JavaFX 贪吃蛇项目在 Git 仓库中**应该上传**与**不应该上传**的文件,供全体成员统一执行。

## 一、应该上传的文件(团队共享)

| 文件/目录 | 说明 |
| ---- | ---- |
| `pom.xml` | **核心**——构建配置、JavaFX 26.0.2 依赖、插件、主类,队友拉下来就能用 |
| `src/main/java/**` | 全部源码:org.example、config、model、util、controller、view 五个包 |
| `src/main/resources/**` | 开发用到的 CSS、字体、图片资源(架构说明书 §8.9 会用到) |
| `.gitignore` | 团队统一的忽略规则 |
| `README.md`、`PRD.md` | 项目文档 |
| `docs/`(如有) | 项目架构说明书、需求文档等团队依据文档 |

> 小例外:`.idea/.gitignore` 这个文件可以提交(它记录 IDE 的通用忽略规则),但 `.idea/` 下其余文件不要提交。

## 二、不应该上传的文件

| 文件/目录 | 原因 |
| ---- | ---- |
| `target/` | 编译产物,每个人自己 build 就有 |
| `.idea/`(workspace.xml、misc.xml 等) | 个人 IDE 配置,已在 .gitignore 中忽略 |
| `.rikki/` | 本地聊天记录,已忽略 |
| `.mvn/`(空目录) | 暂无内容 |
| 本地 Maven 仓库(如 `D:\...\repo`) | 个人环境,含绝对路径 |
| `settings.xml` | 个人 Maven 配置,含本地路径 |
| 运行配置(含绝对路径的) | 换机器即失效 |

## 三、提交规范

1. 提交粒度:一次提交只做一件事(如"完成 Board 类""修复穿墙 bug")
2. 提交信息前缀:
   - `feat:` 新功能
   - `fix:` 修复 bug
   - `docs:` 文档更新
3. 提交前:`git pull` 拉取队友更新 → 本地 `mvn compile` 通过 → 再 push
4. 分工:按架构说明书 A/B/C 模块分工,每人只改自己负责的包,避免同时改同一文件
